# Low load commands - Network/IO intensive (downloads, uploads)
# Maximum concurrent: 32 (default)
#
# Format:
#   - Binary name: curl (matches any path ending with this executable)
#   - Full path: /usr/local/bin/rclone
#   - Directory: /opt/downloaders/ (trailing slash = match any executable in this directory)

curl
wget
rclone
rsync
aria2c
scp
sftp
cadaver
